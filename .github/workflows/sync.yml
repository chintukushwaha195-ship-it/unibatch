name: Auto Sync

on:
  schedule:
    - cron: "*/15 * * * *"   # Every 15 minutes
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Call Sync API
        run: |
          curl -X POST https://unibatch.vercel.app/api/sync
